export interface CreateStationDto {
  name: string;
  city: string;
}

export interface UpdateStationDto {
  name: string;
  city: string;
}

export interface StationResultDto {
  id: string;
  name: string;
  city: string;
}
